# Architecture

Architecture should remain observable, reviewable, and resistant to instruction drift.

Important boundaries, invariants, and execution assumptions should be made explicit rather than hidden in agent context.
