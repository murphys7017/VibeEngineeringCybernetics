# Release Package

Release package contents:

- `AGENTS.md`
- `.ai/index.md`
- `.ai/state.yaml`
- `docs/runtime/release_package.md`
- `tools/validate_runtime.py`

Install by extracting into the target repository root, then run:

```bash
python tools/validate_runtime.py
```

Customize `AGENTS.md` for the target repository if needed.
